// Physical device: point this at your dev machine's LAN IP (phone and PC must
// be on the same Wi-Fi network). Find it with `ipconfig` and update if it changes.
// Using an Android emulator (AVD) instead? Use 'http://10.0.2.2:8080/api'.
const DEV_MACHINE_IP = '10.100.148.176';

// export const API_BASE_URL = `http://${DEV_MACHINE_IP}:8080/api`;
export const API_BASE_URL = 'https://reelsvault-production.up.railway.app/api';
